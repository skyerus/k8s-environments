apiVersion: v1
kind: Service
metadata:
  labels:
    app: riptides-api
    microservice: riptides
  name: riptides-api-service
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: riptides-api
  sessionAffinity: None
  type: NodePort
